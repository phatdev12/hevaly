import { hevaly, hevalyButton } from '../index';

const pad = '1rem 2rem'
const a = new hevaly({
    class: ".hello",
    shadow_type: '15px 15px 15px rgb(0,0,0,0.12)',
    padding: pad,
    margin: '1rem 1rem',
    background: "#000000",
    borderRadius: "30px"
});

const btn = new hevalyButton();